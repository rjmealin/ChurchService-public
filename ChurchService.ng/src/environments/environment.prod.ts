import { EnvironmentModel } from './environment.model';

export const environment: EnvironmentModel = {
    production: true,
    apiUrl: 'https://churchserviceapi.azurewebsites.net',
    test: false,
    maxFileSize: 2097152,
};
